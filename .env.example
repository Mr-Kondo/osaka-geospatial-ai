# Only needed for an optional OpenAI-compatible report endpoint.
# Export this in your shell or use Colab Secrets; never put a key in YAML/notebook output.
LLM_API_KEY=
# Optional Hugging Face token; public default models do not require one.
HF_TOKEN=
